.innerContainer {
    width: 100%;
    align-items: center;
    justify-content: center;
}

.imgContainer {
    width: 430px;
    padding-top: var(--sp-xl);
    margin-bottom: calc(var(--sp-xxl) * -1);

    img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
}
