.container {
  width: 100%;
  gap: var(--sp-md);

  p {
    max-width: 350px;
  }
}

.rowContainer {
  flex-direction: row;
  justify-content: space-between;
}

.md {
}

.lg {
  gap: var(--sp-md);
}

.white {
  color: var(--color-base-white);
}

.black {
  color: var(--color-base-black);
}
